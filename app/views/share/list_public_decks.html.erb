<h2>Public Decks</h2>


<% if !@decks.empty? %>
    <% for deck in @decks do %>
        <div class="deckDescription">
            <h3><%= deck.name %></h3>
            <p>Created by <%= deck.user.login %></p>
            <p>There are <%= deck.words.size %> words</p>
            <h4><%= link_to 'Copy Deck', action: 'copy_deck', id: deck.id %></h4>
        </div>
    <% end %>
<% else %>
    There are no public decks
<% end %>